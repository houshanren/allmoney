/**
 * CATALOG FILTERS
 */

'use strict';

(function () {

	module.exports = angular
		.module('app.filters.catalog', [])
		// TODO: common filters
		.filter('adCity', [function () {

			return function () {
				
				// ...

				return;

			};

		}]);

	// requires

})();